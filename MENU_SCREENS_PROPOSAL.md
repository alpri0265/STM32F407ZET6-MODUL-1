# Аналіз програми та пропозиція екранів меню

## 1. Короткий аналіз програми

- **Платформа:** STM32F407ZET6, LCD 2004 (I2C), 4 рядки × 20 символів.
- **Призначення:** Керування точінням/шліфуванням: дві осі (X, Z), шпиндель, джойстик, MPG/кнопки меню, механічні та програмні обмежувачі, E-Stop, fault.
- **Режими збірки:** `BRINGUP_MODE=1` — тестовий режим з меню на кнопках (PB12/13/14); інакше — робочий режим (planner, safety, без повноцінного меню).
- **Поточне меню (bringup):** Один рівень, 4 пункти: I2C OK, Encoder OK, Settings, Diagnostics. Enter показує "OK: [пункт]", Back — будь-яка кнопка.
- **Джерела даних:** `system_state`, `system_config` (осі X/Z: steps_per_mm, max_feed, min/max mm), `fault`, ADC (temp, feed, tool angle), енкодери, ліміти.

---

## 2. Пропозиція всіх екранів меню

Нижче — повна ієрархія екранів. Умовні позначення:
- **L** — список (курсор, Enter = вхід у підменю або дія).
- **I** — інформаційний екран (тільки перегляд, Back — вихід).
- **V** — екран зміни значення (Up/Down змінюють, Enter — підтвердити, Back — скасувати).

У дужках — вміст рядків LCD (1–4). Усі назви пунктів — латиницею для коректного відображення на LCD.

---

### 2.1 Головне меню (Main) — L

| Рядок | Вміст |
|-------|--------|
| 1 | `> Jog` |
| 2 | `  Settings` |
| 3 | `  Diagnostics` |
| 4 | `  Info` |

- **Enter на "Jog"** → екран Jog.
- **Enter на "Settings"** → підменю Settings.
- **Enter на "Diagnostics"** → підменю Diagnostics.
- **Enter на "Info"** → екран Info.
- **Back** — не застосовується (це корінь).

---

### 2.2 Jog (ручний рух) — I

| Рядок | Вміст |
|-------|--------|
| 1 | `Jog - use joystick` |
| 2 | `X: 0.00 mm` (позиція X з енкодера/stepgen) |
| 3 | `Z: 0.00 mm` (позиція Z) |
| 4 | `[Back]` |

- Тільки перегляд позиції; рух — джойстиком/MPG поза цим екраном.
- **Back** (будь-яка кнопка або окрема логіка) → головне меню.

---

### 2.3 Settings — L

| Рядок | Вміст |
|-------|--------|
| 1 | `> Axis X` |
| 2 | `  Axis Z` |
| 3 | `  Spindle` |
| 4 | `  Save & exit` |

- **Enter на "Axis X"** → екран Axis X params.
- **Enter на "Axis Z"** → екран Axis Z params.
- **Enter на "Spindle"** → екран Spindle (якщо є параметри).
- **Enter на "Save & exit"** → збереження в EEPROM/Flash (якщо реалізовано), повернення в головне меню.
- **Back** → головне меню.

---

### 2.4 Axis X params — L або V

Варіант як список підпараметрів (L):

| Рядок | Вміст |
|-------|--------|
| 1 | `> steps/mm: 400` |
| 2 | `  max feed: 3000` |
| 3 | `  min mm: -200` |
| 4 | `  max mm: 0` |

- Enter на рядок → перехід у режим зміни значення (V): Up/Down змінюють, Enter — OK, Back — скасувати.
- **Back** → Settings.

Варіант одразу як один параметр (V): один екран на параметр (наприклад тільки steps/mm), потім Back — наступний параметр або список.

---

### 2.5 Axis Z params — L

Аналогічно Axis X:

| Рядок | Вміст |
|-------|--------|
| 1 | `> steps/mm: 400` |
| 2 | `  max feed: 2500` |
| 3 | `  min mm: -500` |
| 4 | `  max mm: 0` |

- Логіка та сама: Enter — редагування значення, Back → Settings.

---

### 2.6 Spindle — L або I

Якщо є налаштування (мін/макс PWM або обороти):

| Рядок | Вміст |
|-------|--------|
| 1 | `Spindle settings` |
| 2 | `PWM min/max ...` (або placeholder) |
| 3 | `...` |
| 4 | `[Back]` |

- **Back** → Settings.

Якщо налаштувань немає — можна замінити на інформаційний екран "Spindle: OK" або прибрати пункт.

---

### 2.7 Diagnostics — L

| Рядок | Вміст |
|-------|--------|
| 1 | `> I2C / LCD` |
| 2 | `  Encoders` |
| 3 | `  Limits` |
| 4 | `  ADC / Fault` |

- **Enter на "I2C / LCD"** → екран I2C test.
- **Enter на "Encoders"** → екран Encoders.
- **Enter на "Limits"** → екран Limits.
- **Enter на "ADC / Fault"** → екран ADC & Fault.
- **Back** → головне меню.

---

### 2.8 I2C / LCD test — I

| Рядок | Вміст |
|-------|--------|
| 1 | `I2C/LCD test` |
| 2 | `OK` або `FAIL` (результат останньої перевірки) |
| 3 | `(optional: addr 0x27)` |
| 4 | `[Back]` |

- Можна при Enter запускати одноразову перевірку (запис/читання), оновлювати рядок 2.
- **Back** → Diagnostics.

---

### 2.9 Encoders — I

| Рядок | Вміст |
|-------|--------|
| 1 | `Encoders X / Z` |
| 2 | `X: 12345 cnt` (лічильник або позиція X) |
| 3 | `Z: -6789 cnt` |
| 4 | `[Back]` |

- Дані з `axis_feedback` або тимчасово заглушки.
- **Back** → Diagnostics.

---

### 2.10 Limits — I

| Рядок | Вміст |
|-------|--------|
| 1 | `Limits status` |
| 2 | `X- X+ Z- Z+` |
| 3 | ` 0  0  0  0` (0=open, 1=active) |
| 4 | `[Back]` |

- Читання пінів LIM_X_NEG, LIM_X_POS, LIM_Z_NEG, LIM_Z_POS; оновлення в реальному часі.
- **Back** → Diagnostics.

---

### 2.11 ADC & Fault — I

| Рядок | Вміст |
|-------|--------|
| 1 | `ADC / Fault` |
| 2 | `T: 25.0C F: 0.0 V` (temperature, feed ADC) |
| 3 | `Angle: 0.0 deg` (tool angle) |
| 4 | `Fault: 0` або `Fault: 1` |

- Температура, подача, кут інструменту — з ADC; Fault — з `fault_get()`.
- **Back** → Diagnostics.

---

### 2.12 Info — I

| Рядок | Вміст |
|-------|--------|
| 1 | `State: READY` (або HOLD, ERROR, INIT) |
| 2 | `Fault: 0` |
| 3 | `Build / date` (якщо є) |
| 4 | `[Back]` |

- **Back** → головне меню.

---

## 3. Зведена схема екранів

```
Main (L)
├── Jog (I)
├── Settings (L)
│   ├── Axis X (L/V)
│   ├── Axis Z (L/V)
│   ├── Spindle (L або I)
│   └── Save & exit
├── Diagnostics (L)
│   ├── I2C / LCD (I)
│   ├── Encoders (I)
│   ├── Limits (I)
│   └── ADC / Fault (I)
└── Info (I)
```

**Усього окремих екранів:** 12–14 (залежно від того, чи робити окремі V-екрани для кожного параметра осі).

---

## 4. Що потрібно для реалізації

1. **Меню з рівнями:** стек екранів (screen stack) або `menu_level` + `menu_id`; при Enter — push/перехід, при Back — pop/`menu_back()`.
2. **Типи екранів:** список (L), інфо (I), редагування значення (V). У `screens_process()` визначати тип поточного екрану і малювати відповідний контент (список пунктів / фіксовані рядки / один параметр з Up-Down).
3. **Джерела даних:** підключити читання позицій (stepgen/axis_feedback), лімітів (GPIO), ADC (temperature, feed, tool_angle), fault, system_state.
4. **Збереження налаштувань:** якщо буде EEPROM/Flash — екран "Save & exit" викликає збереження і повертається в Main.
5. **Тексти:** усі підписи на LCD — латиницею, щоб уникнути проблем з кодуванням кирилиці.

Якщо потрібно, можна наступним кроком розписати структури даних (enum screen_id, масиви пунктів, callback Enter/Back) і зміни в `menu.c` / `screens.c` під цю схему.
