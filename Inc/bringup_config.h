#ifndef BRINGUP_CONFIG_H
#define BRINGUP_CONFIG_H

#define BRINGUP_MODE 1

/* 1 = програмні ліміти (SL) увімкнені (навчання, LED, Feed Auto коливання).
 * 0 = вимкнено: навчені межі не враховуються, sl_limits_x/z_taught() завжди false. */
#define SL_LIMITS_ENABLED 1

#endif
