# Чому не працює дисплей LCD 2004 I2C — аналіз і виправлення

## Підсумок: три основні причини

| № | Проблема | Де перевірити | Критичність |
|---|----------|----------------|-------------|
| 1 | **Драйвер LCD — заглушка** | `Src/lcd.c` | Критично |
| 2 | **I2C HAL вимкнено** | `Inc/stm32f4xx_hal_conf.h` | Критично |
| 3 | **I2C2 не ініціалізується** | `Src/main.c` | Критично |

Без виправлення цих трьох речей дисплей **не може** працювати. Нижче — деталі та кроки виправлення.

---

## 1. Драйвер LCD не реалізований (критично)

### Що є зараз

```c
// Src/lcd.c
void lcd_init(void){}
void lcd_print(const char* s){ (void)s; }
```

- `lcd_init()` нічого не робить — дисплей не ініціалізується.
- `lcd_print()` нічого не робить — текст нікуди не виводиться.

Тобто на рівні коду дисплей взагалі не використовується.

### Що потрібно

Потрібна повноцінна реалізація для **LCD 2004 I2C** (контролер HD44780, інтерфейс через PCF8574 по I2C):

1. **У `lcd_init()`:**
   - Ініціалізація I2C (якщо використовується окремо від `main`).
   - Затримка після живлення (≥40–50 мс).
   - Послідовність ініціалізації HD44780 (4-бітний режим) через PCF8574 (запись по I2C у правильні регістри з бітами RS, E, Backlight тощо).
   - Для 20×4 — правильні адреси рядків: 0x00, 0x40, 0x14, 0x54.

2. **У `lcd_print()` (або допоміжних функціях):**
   - Вибір рядка (0–3).
   - Відправка символів по I2C (4-бітна передача в HD44780 через PCF8574).

Без такого драйвера дисплей не загориться і нічого не покаже, навіть якщо I2C працює.

---

## 2. Модуль HAL I2C вимкнено (критично)

### Що є зараз

У `Inc/stm32f4xx_hal_conf.h`:

```c
/* #define HAL_I2C_MODULE_ENABLED */
```

I2C у HAL вимкнено — функції типу `HAL_I2C_Transmit()` не збираються і не можуть бути використані драйвером LCD.

### Що зробити

1. Відкрити `Inc/stm32f4xx_hal_conf.h`.
2. Увімкнути I2C:
   - Знайти рядок `/* #define HAL_I2C_MODULE_ENABLED */`.
   - Замінити на: `#define HAL_I2C_MODULE_ENABLED`.
3. Зберегти і перезібрати проєкт.

Після цього HAL I2C буде доступний для реалізації драйвера LCD по I2C2.

---

## 3. Периферія I2C2 не ініціалізується (критично)

### Що є зараз

- У `main.c` викликаються лише: `MX_GPIO_Init()`, `MX_ADC1_Init()`, `MX_TIM1_Init()`.
- Функції **`MX_I2C2_Init()` немає** — I2C2 ніде не налаштовується і не увімкується.
- Піни PB10 (SCL) та PB11 (SDA) в `MX_GPIO_Init()` налаштовані як AF для I2C2, але сама периферія I2C2 не ініціалізована.

Наслідок: на виводах немає коректної роботи I2C, дисплей не отримує команд і даних.

### Що зробити

**Варіант A — через STM32CubeMX (бажано):**

1. Відкрити `.ioc` проєкту.
2. Увімкнути **I2C2**.
3. Прив’язати до пінів: **PB10 — I2C2_SCL**, **PB11 — I2C2_SDA**.
4. Згенерувати код (Generate Code).
5. Переконатися, що в `main.c` з’явився виклик `MX_I2C2_Init()` і він виконується після `MX_GPIO_Init()` (наприклад, разом з іншими `MX_*_Init()`).

**Варіант B — вручну додати ініціалізацію I2C2:**

- Додати в проєкт файли HAL для I2C (якщо їх ще немає):  
  `stm32f4xx_hal_i2c.c` / `stm32f4xx_hal_i2c.h` (або використовувати згідно з вашим HAL).
- Оголосити хендл: наприклад `I2C_HandleTypeDef hi2c2;`.
- Реалізувати функцію `MX_I2C2_Init()` (швидкість, режим, таймаути тощо) і викликати її в `main()` після інших ініціалізацій периферій.

Поки I2C2 не ініціалізований і не викликається `MX_I2C2_Init()`, дисплей по I2C працювати не буде.

---

## 4. Можливі апаратні та налаштувальні проблеми

Перевірити після того, як виправлено пункти 1–3.

### 4.1. Адреса I2C дисплея

- Типові адреси модуля з PCF8574: **0x27** або **0x3F** (залежить від A0–A2 на платі).
- Якщо в коді використовується інша адреса — дисплей не відповість.
- Можна перевірити адресу сканером I2C на шині (наприклад, вивід усіх знайдених адрес у діапазоні 0x08–0x77).

### 4.2. Підтяжки на SCL та SDA

- На шині I2C обов’язково мають бути підтяжки (зазвичай 4.7 кОм до 3.3 V) на SCL і SDA.
- Якщо модуль LCD 2004 вже має підтяжки на платі — додаткові на платі STM32 можуть не бути потрібні, але шина має бути однією із підтяжкою з боку хоча б одного пристрою.

### 4.3. Живлення та підключення

- Напруга живлення дисплея: 5 V або 3.3 V — згідно з даташитом вашого модуля.
- Логіка: якщо дисплей 5 V, а STM32 — 3.3 V, переконатися, що рівні сумісні (багато модулів 2004 приймають 3.3 V логіку).
- Перевірити проводку: **PB10 → SCL**, **PB11 → SDA**, GND спільний, живлення дисплея відповідно до інструкції.

### 4.4. Затримка після включення

- HD44780 потребує після подачі живлення затримки (типово ≥40–50 мс) перед початком ініціалізації.
- Якщо `lcd_init()` викликається одразу після старту МК — додати на початку `lcd_init()` затримку, наприклад `HAL_Delay(50);`.

### 4.5. Контраст

- На модулі зазвичай є підстроювач контрасту. Якщо контраст викручено в мінімум, екран може здаватися «не робочим» при повністю коректному програмному виводі.

---

## 5. Порядок дій (чек-лист)

1. **Увімкнути I2C у HAL**  
   `stm32f4xx_hal_conf.h` → `#define HAL_I2C_MODULE_ENABLED`

2. **Додати та викликати ініціалізацію I2C2**  
   У `main.c`: з’явитися `MX_I2C2_Init()` і її виклик після інших `MX_*_Init()` (найпростіше — згенерувати через CubeMX).

3. **Реалізувати драйвер LCD 2004 I2C у `lcd.c`**  
   - `lcd_init()`: затримка після живлення, ініціалізація HD44780 через PCF8574 по I2C2 (PB10/PB11), правильні адреси рядків для 20×4.  
   - `lcd_print()` (та при потребі додаткові функції): вибір рядка, передача символів по I2C.

4. **Перевірити апаратуру**  
   Адреса I2C (0x27/0x3F), підтяжки, проводка SCL/SDA, живлення, контраст.

5. **Зібрати проєкт і прошити плату**  
   Після змін перезібрати без помилок і перепрошити МК.

---

## 6. Висновок

- **Дисплей не працює**, тому що:  
  - драйвер LCD у проєкті — порожня заглушка;  
  - I2C у HAL вимкнено;  
  - I2C2 не ініціалізується в `main.c`.  

Після увімкнення HAL I2C, додавання ініціалізації I2C2 і реалізації драйвера для LCD 2004 I2C (PB10 SCL, PB11 SDA) дисплей має почати працювати; якщо ні — перевірити апаратну частину та адресу за цим документом.
