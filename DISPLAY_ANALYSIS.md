# Аналіз дисплея в проєкті STM32F407ZET6-MODUL-1

## 1. Архітектура підсистеми дисплея

```
app_loop()
    └── screens_process()
            └── lcd_print("<стан системи>")

screens_init()
    └── lcd_init()
```

- **screens** — рівень екранів: визначає, *що* показувати (залежить від стану системи).
- **lcd** — рівень драйвера: визначає, *як* виводити текст на фізичний дисплей (зараз — заглушка).

---

## 2. Поточний стан реалізації

### 2.1. Екрани (screens)

| Файл      | Призначення |
|-----------|-------------|
| `Inc/screens.h` | Прототипи `screens_init()`, `screens_process()` |
| `Src/screens.c` | Ініціалізація та циклічне оновлення екрану за станом системи |

**Логіка виводу:**
- У `screens_process()` викликається `system_state_get()`.
- Залежно від стану викликається `lcd_print()` з одним із рядків:
  - `SYS_STATE_INIT`  → `"INIT"`
  - `SYS_STATE_READY` → `"READY"`
  - `SYS_STATE_HOLD`  → `"HOLD"`
  - `SYS_STATE_ERROR` → `"ERROR"`

Тобто на дисплеї задумано показувати лише **поточний стан системи** (одним рядком).

### 2.2. Драйвер LCD (lcd)

| Файл      | Зміст |
|-----------|--------|
| `Inc/lcd.h` | `lcd_init()`, `lcd_print(const char* s)` |
| `Src/lcd.c` | Реалізація-заглушка: нічого не робить |

```c
// Src/lcd.c (поточний код)
void lcd_init(void){}
void lcd_print(const char* s){ (void)s; }
```

Висновок: **фізичний дисплей поки не використовується** — ініціалізація та вивід тексту не реалізовані, тільки інтерфейс для підключення справжнього драйвера.

---

## 3. Апаратна частина (розпіновка)

### 3.1. Дисплей та інтерфейс

**Дисплей:** LCD 2004 I2C (20×4 символи, інтерфейс I2C, зазвичай через PCF8574).

У проєкті під дисплей виведено **I2C2**:

| Сигнал   | Пін  | Портал | Режим        | Опис                    |
|----------|------|--------|-------------|-------------------------|
| SCL      | PB10 | GPIOB  | AF (I2C2)   | Тактовий I2C (LCD 2004) |
| SDA      | PB11 | GPIOB  | AF (I2C2)   | Дані I2C (LCD 2004)     |

У `main.c` піни PB10/PB11 налаштовані як **AF Open-Drain** для I2C2.  
У `board.h` задано: `LCD_I2C I2C2`, `LCD_I2C_SCL_PORT/PIN` (PB10), `LCD_I2C_SDA_PORT/PIN` (PB11).

### 3.2. Чи ініціалізується I2C у проєкті

- У `main.c` **немає** виклику `MX_I2C2_Init()` (або аналогічного).
- Налаштовані лише піни PB10/PB11 під I2C; периферія I2C2 у коді не вмикається.

Щоб дисплей працював, потрібно:
1. Увімкнути I2C2 у CubeMX/конфігурації та додати ініціалізацію в `main.c`.
2. Реалізувати в `lcd.c` драйвер для конкретного дисплея (див. розділ 5).

---

## 4. Функціональні вимоги до дисплея

З коду випливає:

- Вивід тексту стану системи (одним або кількома рядками).
- Оновлення в циклі `screens_process()` при зміні стану.
- Інтерфейс: лише `lcd_init()` та `lcd_print(const char *)`.

**Використовується дисплей:** **LCD 2004 I2C** (20×4 символи), підключення по I2C2: **PB10 (SCL)**, **PB11 (SDA)**. Зручно реалізувати через драйвер для HD44780 + PCF8574 (I2C).

---

## 5. Рекомендації для реалізації драйвера

### 5.1. LCD 2004 I2C (20×4), PB10 (SCL), PB11 (SDA)

- Дисплей: **LCD 2004 I2C** — HD44780-сумісний, 20×4 символи, інтерфейс I2C (часто через PCF8574).
- Піни: **PB10 = SCL**, **PB11 = SDA**, периферія **I2C2**.
- Адреса I2C PCF8574: зазвичай **0x27** або **0x3F** (залежить від A0–A2 на модулі).
- Алгоритм:
  1. У CubeMX увімкнути I2C2 (PB10, PB11), згенерувати `MX_I2C2_Init()`.
  2. У `lcd_init()`: ініціалізація HD44780 через 4-бітну шину та PCF8574 (послідовність по даташиту); для 20×4 — правильні адреси рядків (0x00, 0x40, 0x14, 0x54).
  3. У `lcd_print(const char* s)`: вивести `s` у поточний рядок; при потребі використовувати 4 рядки для розширеного виводу.

### 5.2. Конфігурація в board.h

- У `board.h` задано: `LCD_I2C I2C2`, `LCD_I2C_SCL_PORT/PIN` (PB10), `LCD_I2C_SDA_PORT/PIN` (PB11). У коді драйвера використовувати ці макроси для підключення до I2C2 та пінів.

### 5.3. Захист від частих оновлень

Зараз `screens_process()` викликається що цикл і щоразу викликає `lcd_print()`. Щоб не мерехтіло та не навантажувати I2C:

- Зберігати останній виведений стан і викликати `lcd_print()` лише при зміні стану, або
- Додати невелику затримку/лічильник між оновленнями дисплея (наприклад, раз на 100–200 мс).

---

## 6. Підсумкова таблиця

| Аспект              | Стан |
|---------------------|------|
| Дисплей             | LCD 2004 I2C (20×4) |
| Розпіновка I2C      | PB10 (SCL), PB11 (SDA), I2C2 |
| Конфігурація board.h| LCD_I2C = I2C2, макроси для PB10/PB11 |
| Ініціалізація I2C2  | Відсутня в main.c (потрібно додати) |
| Логіка екранів      | Реалізована (INIT/READY/HOLD/ERROR) |
| Драйвер LCD         | Заглушка (потрібна реалізація для 2004) |

---

## 7. Висновок

- **Дисплей:** LCD 2004 I2C (20×4), підключення **PB10 (SCL)**, **PB11 (SDA)**, шина **I2C2**.
- **Архітектура:** є рівень екранів і інтерфейс `lcd_init`/`lcd_print`; драйвер у `lcd.c` поки заглушка.
- **Щоб дисплей працював:** увімкнути та ініціалізувати I2C2 у проєкті, реалізувати в `lcd.c` драйвер для LCD 2004 по I2C (HD44780 + PCF8574).

Цей документ можна використовувати як технічний опис та план впровадження дисплея в проєкті.
